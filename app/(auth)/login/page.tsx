import LoginForm from "@/components/forms/loginForm";

function LoginPage() {
  return (
    <main className="mx-auto min-h-screen w-full flex items-center justify-center">
      <LoginForm />
    </main>
  );
}

export default LoginPage;
